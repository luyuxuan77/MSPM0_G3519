# 工程规则

## 头文件引用规则

1. `BSP` 下各模块头文件禁止直接包含聚合头 `bsp.h`。
2. `BSP` 下各模块头文件默认包含 `BSP/bsp_common.h`，只依赖公共基础能力。
3. 只有应用层文件，或者确实需要一次性接入全部模块的文件，才允许包含 `BSP/bsp.h`。
4. 源文件应优先包含自己对应的模块头文件；只有在确实需要多个模块接口时，才补充包含其他头文件。

## 注释与编码规则

1. 从现在开始，所有被检查过且发生修改的代码，都必须补充详细中文注释。
2. 注释应说明代码块的职责、实现原因、关键输入输出以及硬件或时序约束。
3. 新增或修改后的源码文件、头文件统一使用 `UTF-8` 编码保存。
4. 若后续修改到存在乱码注释的旧文件，应一并整理为正常中文注释，并保持 `UTF-8` 编码。